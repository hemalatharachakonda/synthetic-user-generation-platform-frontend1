# Backend that stores everything created in this app (experiments, personas,
# survey answers, interviews, insights, reports). Defaults to the deployed
# instance below if not set.
BACKEND_URL=https://synthetic-ai-user-backend-deployment-2.onrender.com

# Optional — only needed if you still want local/offline generation as a
# manual fallback; not used by the backend-connected flow anymore.
# GROQ_API_KEY=
